[
  {
    "id": 11,
    "title": "My Favorite Color",
    "type": "coding",
    "difficulty": "easy",
    "tags": ["compile"],
    "description": "You’ve been provided with several Java source files.\n\n### Your Task\n\nExamine the given code files carefully and identify **which types of issues** are present in the project.  \nSome files may contain **one** type of error, while others may include **multiple**.\n\nSelect **all** applicable options for each file:\n\n- 🧱 Compile-Time Error\n- 💥 Runtime Error\n- 🧮 Logic Error\n- 🔐 Vulnerability\n\n---\n\n## ⚙️ Types of Errors & Examples\n\n### 🧱 1. Compile-Time Errors\n\nThese are **detected by the compiler** before the program runs.  \nThey prevent the code from compiling successfully.\n\n**Common causes:**\n\n- Missing semicolons or braces\n- Type mismatches\n- Undefined variables or methods\n- Syntax violations\n\n**Example:**\n\n```java\npublic class Demo {\n    public static void main(String[] args) {\n        int x = 5\n        System.out.println(x);\n    }\n}\n```\n\n🚫 _Error:_ `';' expected` → The compiler stops execution until the syntax is fixed.\n\n---\n\n### 💥 2. Runtime Errors\n\nThese occur **while the program is running**, after successful compilation.\nThey typically cause the program to crash or terminate abnormally.\n\n**Common causes:**\n\n- Division by zero\n- Null pointer access\n- Array index out of bounds\n- File not found\n\n**Example:**\n\n```java\npublic class CrashExample {\n    public static void main(String[] args) {\n        int[] numbers = {1, 2, 3};\n        System.out.println(numbers[5]); // Index out of bounds\n    }\n}\n```\n\n💥 _Error:_ `ArrayIndexOutOfBoundsException` → Program compiles but fails during execution.\n\n---\n\n### 🧮 3. Logic Errors\n\nThe program **runs without crashing**, but produces **incorrect or unintended results**\nbecause of a mistake in logic or algorithm design.\n\n**Common causes:**\n\n- Incorrect condition in an `if` or loop\n- Wrong arithmetic or comparison\n- Misplaced return statement\n\n**Example:**\n\n```java\npublic class LogicExample {\n    public static boolean isEven(int n) {\n        return n % 2 == 1; // ❌ Incorrect logic — should be n % 2 == 0\n    }\n\n    public static void main(String[] args) {\n        System.out.println(isEven(4)); // Prints false instead of true\n    }\n}\n```\n\n🧩 _Error:_ The code runs fine but produces the wrong output.\n\n---\n\n### 🔐 4. Vulnerabilities (Security Issues)\n\nThese are **weaknesses** in the code that could be exploited,\neven though the program may still compile and run correctly.\n\n**Common causes:**\n\n- Hardcoded credentials or secrets\n- Using insecure APIs (e.g., `Runtime.exec()` with user input)\n- Lack of input validation (SQL injection, command injection)\n- Weak encryption or no authentication\n\n**Example:**\n\n```java\npublic class VulnerableExample {\n    public static void main(String[] args) {\n        String userInput = \"../etc/passwd\"; // Imagine this comes from a form\n        Runtime.getRuntime().exec(\"cat \" + userInput); // 🚨 Command injection\n    }\n}\n```\n\n🔓 _Vulnerability:_ Unsanitized user input allows arbitrary command execution.\n\n---\n\n### ✅ Summary\n\n| Type                   | Detected When             | Program Runs? | Example Symptom    |\n| ---------------------- | ------------------------- | ------------- | ------------------ |\n| **Compile-Time Error** | During compilation        | ❌ No         | Code won’t compile |\n| **Runtime Error**      | During execution          | ⚠️ Crashes    | Exception thrown   |\n| **Logic Error**        | During execution          | ✅ Yes        | Wrong output       |\n| **Vulnerability**      | During or after execution | ✅ Yes        | Security risk      |\n\n---\n\n🧭 **Goal:**\nUse your detective skills to pinpoint which errors or bugs are present in each file — and select all that apply!\n"
  }
]
